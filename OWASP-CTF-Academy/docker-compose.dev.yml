services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ctf_platform
      POSTGRES_USER: ctf
      POSTGRES_PASSWORD: ctf_password
    ports: ["5432:5432"]
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
